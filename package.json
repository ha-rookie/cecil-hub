{
  "name": "cecil-hub",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:icons": "node scripts/generate_app_icons.cjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
